// a) usaria o comando "tsc" no console

// b) Não, já que o comando "tsc" faz a transpilação de todos os arquivos ts

// c) tsc

// d) algumas propriedades são descomentadas ou mudadas, como a versão do target, outDir e rootDir